# Root ignore for the whole monorepo
node_modules/
.env
server/node_modules/
server/.env
server/*.log
server/backups/
crash_state.json
.DS_Store

# Separate embedded repo, not part of deployment
blackjack-game/
